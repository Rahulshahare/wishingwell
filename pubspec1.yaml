name: wish_well_i
description: Arcade mining‑collector game.
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.14.0 # game engine
  flutter_svg: ^2.0.7 # optional for SVG assets
  anchor: ^0.0.7 # simple physics helpers (optional)
  shared_preferences: ^2.2.2 # for persistence
dependency_overrides:
  meta: 1.15.0

flutter:
  uses-material-design: true
  assets:
    - assets/background.png
    - assets/bucket.png
    - assets/rock.png
    - assets/coin.png
    - assets/panel.png
    - assets/particle.png